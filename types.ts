export type Point2D = {
  x: number;
  y: number;
};

export type Point3D = {
  x: number;
  y: number;
  z: number;
};

export type PointPolar = {
  r: number;
  theta: number;
};

export type PointSpherical = {
  r: number;
  theta: number;
  phi: number;
};
